#Localy generate certificate
#Copy files to remote host from inventory
#Generater certificates on remote host
- name: venafi_certificate
  tasks:
  - name: venafi_certificate
    local_action:
      module: venafi_certificate
      url: "{{ venafi.url }}"
      token: "{{ venafi.token }}"
      zone: "{{ venafi.zone }}"
      path: "{{ path }}"
      test_mode: "{{ venafi.test_mode }}"
      user: "{{ venafi.user }}"
      password: "{{ venafi.password }}"
      common_name: "{{ common_name }}"
    register: testout
  - name: "dump test output"
    debug:
      msg: "{{ testout }}"
